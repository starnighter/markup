{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability: window + shell + dialog open",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
